<UserControl x:Class="pacman.menuCb"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lcl="clr-namespace:pacman"
    mc:Ignorable="d"
    d:DesignHeight="50" d:DesignWidth="247">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="0.1*" />
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<CheckBox VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="cb" IsChecked="{Binding isChecked, Mode=TwoWay}" />
		<lcl:Outline x:Name="txt" text="0" Foreground="Red" Grid.Column="1"/>
	</Grid>
</UserControl>
